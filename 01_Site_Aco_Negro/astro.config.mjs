import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // O link base do GitHub Pages para não quebrar caminhos de imagens/CSS
  site: 'https://MarcosP-Costa.github.io',
  base: '/Aco_Negro_Swordplay',
});
